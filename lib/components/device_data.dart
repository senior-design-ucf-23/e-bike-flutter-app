import 'package:flutter_reactive_ble/flutter_reactive_ble.dart';

// device data class to transfer device info between pages
class DeviceData {
  final DiscoveredDevice device;

  DeviceData(this.device);
}
